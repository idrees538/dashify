{
    "version": 2,
    "buildCommand": "cd client && npm install && npm run build && cd ../admin && npm install && npm run build && cd .. && rm -rf dist && mkdir dist && cp -r client/dist/* dist/ && mkdir -p dist/admin && cp -r admin/dist/* dist/admin/",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.js"
        },
        {
            "source": "/admin/(.*)",
            "destination": "/admin/index.html"
        },
        {
            "source": "/((?!admin/).*)",
            "destination": "/index.html"
        }
    ]
}